# Secretos — NUNCA subir
.env

# Entorno virtual
.venv/

# Datos descargados y cachés locales
data/raw/
data/processed/
*.db
*.db-wal
*.db-shm

# EXCEPCIÓN: la base de clubes SÍ se sube (13 MB). Reconstruirla desde ESPN tarda
# 10-20 minutos, y Streamlit Cloud no aguanta eso en el arranque; con la base
# versionada la app abre al instante y solo refresca la temporada en curso.
!data/clubes.db

# Python
__pycache__/
*.pyc

# Secretos de Streamlit (el theme .streamlit/config.toml SÍ se sube)
.streamlit/secrets.toml

# Configuración local de Claude Code / preview
.claude/
